name: Sync canonical Vast.ai skills

on:
  schedule:
    - cron: "17 4 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    uses: vast-ai/skills/.github/workflows/sync-vastai-wrapper.yml@main
    with:
      base-branch: main
    secrets: inherit
