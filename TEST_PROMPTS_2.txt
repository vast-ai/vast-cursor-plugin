# Re-test prompts for the post-live-test skill edits.
#
# Run these in a FRESH claude session (or after /reload-plugins) so the
# agent picks up the new skill content. Paste one prompt at a time.
#
# Run against a LIVE Vast.ai account. Do NOT verify by reading --help.
#
# Placeholders — substitute REAL values; never use fake numbers.
#
# <ENDPOINT_ID>       → real endpoint id from `vastai show endpoints --raw`
#                       (requires admin-scope key + deposited balance)
# <TEMPLATE_HASH>     → real hash from `vastai search templates ... --raw`,
#                       or a hash you own from `vastai show templates --raw`


Create a serverless workergroup for endpoint <ENDPOINT_ID> using template hash <TEMPLATE_HASH>.

Benchmark template <TEMPLATE_HASH> across 1, 2, and 4 GPUs.

Delete template <TEMPLATE_HASH>.
