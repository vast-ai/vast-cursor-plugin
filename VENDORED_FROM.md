# Vendored from `vast-ai/vast-cli`

`skills/vastai/SKILL.md` is copied from `vast-ai/vast-cli` at:

- **Path:** `vastai/SKILL.md`
- **SHA:** `13dbf3904fa917d29cd23f6e1de087d56dead958`
- **Subject:** tell user which api key used on 401 error, group --help commands (#400)
- **Vendored:** 2026-05-18

The vendored copy carries local patches on top of the upstream SHA — see [PATCHES.md](./PATCHES.md). When upstream merges the patches, re-vendor and remove the local deltas.

## Re-vendoring

```bash
cp /path/to/vast-cli/vastai/SKILL.md skills/vastai/SKILL.md
```

Then re-apply patches from `PATCHES.md` if upstream still lacks them, and update the SHA above.

Do not hand-edit `skills/vastai/SKILL.md` — changes must land upstream in `vast-ai/vast-cli` first.
