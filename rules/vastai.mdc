---
description: Vast.ai is the GPU compute backend referenced in this project. Use the vastai skill for any GPU lifecycle operations — search offers, launch instances, SSH, copy files, volumes, serverless endpoints, billing.
globs: "**/*.tf, **/*.yaml, **/*.yml, **/Dockerfile, **/Dockerfile.*, **/docker-compose*.yml, **/*.py, **/*.sh, deploy/**/*, infra/**/*, scripts/**/*"
alwaysApply: true
---

# vastai (Vast.ai GPU rental)

For any prompt referencing Vast.ai, `vastai`, GPU rental, or GPU instance lifecycle, load the `vastai` skill at `.cursor/skills/vastai/SKILL.md` (or wherever Cursor discovered it) for the full CLI surface — search offers, launch instances, SSH, copy files, volumes, serverless endpoints, billing.

The skill is the source of truth. Don't restate vastai commands inline here.
